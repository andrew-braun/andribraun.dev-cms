# Run linting and formatting
# Create migration file (CRITICAL - must run before every commit)
echo "Creating Payload migration..."
pnpm payload migrate:create
git add src/migrations

# Run linting and formatting on the staged set, including generated migrations
pnpm exec lint-staged
